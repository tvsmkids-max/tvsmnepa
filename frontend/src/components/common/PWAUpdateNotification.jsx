/**
 * PWAUpdateNotification — Disabled (Silent Auto-Update Mode)
 * The app now updates automatically in the background without popups.
 */
const PWAUpdateNotification = () => {
  return null;
};

export default PWAUpdateNotification;
